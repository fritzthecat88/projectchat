name: streamlit_env
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - streamlit
      - openai
      - langchain==0.1.20
      - langchain-openai==0.0.7
      - pydantic>=2.7
      - langgraph
      - json-logic
      - commentjson
