name: streamlit_env
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - streamlit
  - openai
  - pip:
      - langchain
      - langchain-openai  
      - pydantic>=2.7
      - langgraph
      - json-logic
